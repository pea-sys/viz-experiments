[build-system]
requires = ["setuptools>=40.6.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "my_component_name"
version = "0.1"
dependencies = ["shiny >= 0.6.0"]

[tool.setuptools]
packages = ["my_component_name"]
include-package-data = true
zip-safe = false
